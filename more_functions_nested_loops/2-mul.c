#include "main.h"
/**
 * mul - Check if the letter is an uppercase or not
 * @a: The letter
 * @b: The second parameter
*
 * Return: Return 1 if its through 0 and 9
 */
int mul(int a, int b)
{
	return (a * b);
}
