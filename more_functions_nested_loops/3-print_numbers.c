#include "main.h"
/**
 * print_numbers - Check if the letter is an uppercase or not
*
 * Return: Return 1 if its through 0 and 9
 */
void print_numbers(void)
{
int n;

	for (n = 48; n <= 57; n++)
	{
		_putchar(n);
	}
	_putchar('\n');
}
