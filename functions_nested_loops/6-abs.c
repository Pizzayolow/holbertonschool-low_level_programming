#include "main.h"
/**
 * _abs - My function calculate the absolute of a number
 * @i: i initialize the variable(number)
 *
 * Return: Return 0 if not
 */
int _abs(int i)
{
	if (i < 0)
	{
		i = i * -1;
	}
		else
		{
		}

	return (i);
}
